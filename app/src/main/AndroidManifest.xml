<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:name=".SecureApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureFolder"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.SignupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.login.RecoveryCodeActivity" />
        <activity android:name=".ui.login.ForgotPasswordActivity" /> <activity android:name=".MainActivity" />
        <activity android:name=".ui.modules.PhotosActivity" />
        <activity android:name=".ui.modules.VideosActivity" />
        <activity android:name=".ui.modules.DocumentsActivity" />
        <activity android:name=".ui.modules.NotesActivity" />
        <activity android:name=".ui.modules.PasswordsActivity" />
        <activity android:name=".ui.modules.TrashActivity" />
        <activity android:name=".ui.modules.SettingsActivity" />
        <activity android:name=".ui.modules.PhotoViewerActivity" />
        <activity android:name=".ui.modules.VideoViewerActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />
        <activity android:name=".ui.modules.DocumentViewerActivity" />
        <activity android:name=".ui.modules.NoteEditorActivity" />
        <activity android:name=".ui.modules.PasswordEditorActivity" />

    </application>

</manifest>